Полиморфизм - способность вызывать метод от разных аргументов 

Язык с++ распологает набором фундаментальных типов, соотвествуещих наиболее общим еденицам компьютера для хранения данных, а также наиболее общим способам использования данных

Логический тип bool имеет всего лишь два значения: true и folse. Этот тип используется для отражения результатов логических операций.

unsingned char: представляет один символ. Занимает в памяти 1 байт (8 бит). Может хранить любое значение из диапозона от 0 до 255

Static в нутри определения класса. Полезно преставить себе, что статические переменные  класса содержат информацию, необходимую  для создания новых объектов (например в фабрике классов). Напримеремер, если вы хотите  пронумеровать экземпляры 

Линковка библлиотеки - это указание в компилируемом файле адресов на методы и члены из линкуемой библиотеки
